{
  "description": "Integration Speciment",
  "private": true,
  "devDependencies": {
    "gh-pages": "^1.2.0",
    "gitbook-cli": "^2.3.2"
  },
  "scripts": {
    "": "",
    "build": "node_modules/.bin/gitbook build",
    "serve": "node_modules/.bin/gitbook serve",
    "clean": "rm -rf _book/*",
    "open": "open _book/index.html",
    "dist": "node_modules/.bin/gh-pages -d _book",
    "predist": "npm run build",
    "prebuild": "make all"
  }
}
